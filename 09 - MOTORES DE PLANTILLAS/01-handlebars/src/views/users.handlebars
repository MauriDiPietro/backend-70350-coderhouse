{{#if users.length}}
  {{#each users}}
    <ul>
      <li>{{firstname}}</li>
      <li>{{lastname}}</li>
      <li>{{mail}}</li>
      <li>{{age}}</li>
      <li>{{phone}}</li>
    </ul>
  {{/each}}
{{else}}
  <p>No hay usuarios</p>
{{/if}}
<script src="index.js"></script>